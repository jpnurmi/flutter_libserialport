name: flutter_serial_port_example
description: Demonstrates how to use the flutter_serial_port plugin.
publish_to: 'none'

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_serial_port:
    path: ../

flutter:
  uses-material-design: true
