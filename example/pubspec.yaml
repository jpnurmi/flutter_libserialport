name: flutter_serialport_example
description: Demonstrates how to use the flutter_serialport plugin.
publish_to: 'none'

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_serialport:
    path: ../

flutter:
  uses-material-design: true
